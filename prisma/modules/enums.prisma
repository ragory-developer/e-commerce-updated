// ========== Auth & Identity ==========
enum AdminRole {
  SUPERADMIN
  ADMIN
  MANAGER
}

enum AdminPermission {
  MANAGE_USERS
  VIEW_USERS
  MANAGE_PRODUCTS
  VIEW_PRODUCTS
  MANAGE_ORDERS
  VIEW_ORDERS
  MANAGE_PAYMENTS
  VIEW_PAYMENTS
  VIEW_REPORTS
  EXPORT_DATA
  MANAGE_SETTINGS
  MANAGE_COUPONS
  MANAGE_SHIPPING
  MANAGE_DELIVERY
}

enum AuthUserType {
  ADMIN
  CUSTOMER
}

enum OtpChannel {
  EMAIL
  SMS
}

enum OtpPurpose {
  VERIFY_PHONE
  VERIFY_EMAIL
  RESET_PASSWORD
  LOGIN_OTP
}

// ========== Product ==========
enum AttributeType {
  TEXT
  NUMBER
  SELECT
  MULTI_SELECT
  COLOR_PICKER
  CHECKBOX
  DATE_PICKER
}

enum SpecialPriceType {
  FIXED
  PERCENT
}

enum VariationType {
  TEXT
  COLOR
  IMAGE
}

enum PublishStatus {
  DRAFT
  PUBLISHED
  SCHEDULED
}

// ========== Order & Payment ==========
enum OrderStatus {
  PENDING
  PROCESSING
  COMPLETED
  DECLINED
  CANCELED
  REFUNDED
  PARTIALLY_REFUNDED
  ON_HOLD
  SHIPPED
  DELIVERED
}

enum PaymentMethod {
  BKASH
  NAGAD
  SSLCOMMERZ
  COD
  BANK_TRANSFER
  STRIPE
  PAYPAL
}

enum ShippingMethod {
  FREE_SHIPPING
  LOCAL_PICKUP
  FLAT_RATE
}

// ========== Coupon ==========
enum CouponDiscountType {
  FIXED
  PERCENT
}

// ========== Tax ==========
enum TaxBasedOn {
  BILLING_ADDRESS
  SHIPPING_ADDRESS
  STORE_ADDRESS
}

// ========== File ==========
enum FileStorage {
  LOCAL
  S3
  CLOUDINARY
  GCS
}

// ========== Audit ==========
enum AuditAction {
  CREATE
  UPDATE
  DELETE
  RESTORE
}

// ========== Option ==========
enum OptionType {
  DROPDOWN
  CHECKBOX
  RADIO
  MULTIPLE_SELECT
  TEXT
  TEXTAREA
  DATE
  TIME
  DATE_TIME
  FILE
}

enum OptionPriceType {
  FIXED
  PERCENT
}
